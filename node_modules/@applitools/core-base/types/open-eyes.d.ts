import type { OpenSettings, Eyes } from './types';
import { type Logger } from '@applitools/logger';
import { type CoreRequests } from './server/requests';
declare type Options = {
    requests: CoreRequests;
    logger: Logger;
    cwd: string;
};
export declare function makeOpenEyes({ requests, logger: defaultLogger, cwd }: Options): ({ settings, logger }: {
    settings: OpenSettings;
    logger?: Logger;
}) => Promise<Eyes>;
export {};
