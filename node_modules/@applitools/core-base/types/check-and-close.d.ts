import type { Target, CheckSettings, CloseSettings, TestResult } from './types';
import { type Logger } from '@applitools/logger';
import { type EyesRequests } from './server/requests';
declare type Options = {
    requests: EyesRequests;
    logger: Logger;
};
export declare function makeCheckAndClose({ requests, logger: defaultLogger }: Options): ({ target, settings, logger, }: {
    target: Target;
    settings?: CheckSettings & CloseSettings;
    logger?: Logger;
}) => Promise<TestResult[]>;
export {};
